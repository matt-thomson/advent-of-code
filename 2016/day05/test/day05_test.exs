defmodule Day05Test do
  use ExUnit.Case

  test "solves part one" do
    assert Day05.part_one("abc") == "18f47a30"
  end

  test "solves part two" do
    assert Day05.part_two("abc") == "05ace8e3"
  end
end
